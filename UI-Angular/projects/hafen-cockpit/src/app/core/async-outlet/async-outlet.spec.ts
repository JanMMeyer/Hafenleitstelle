import { AsyncOutlet } from './async-outlet';

describe('AsyncOutlet', () => {
	it('should create an instance', () => {
		const directive = new AsyncOutlet();
		expect(directive).toBeTruthy();
	});
});
